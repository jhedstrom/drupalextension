# Starter Behat configuration for the Drupal Extension.
#
# Copy this file to behat.yml and adjust values for your project.
#
# This file lists all available configuration options with their default
# values for reference. Customize as needed.

# Blackbox profile: tests against a Drupal site without bootstrapping Drupal.
default:
  suites:
    default:
      contexts:
        - Drupal\DrupalExtension\Context\MinkContext
        - Drupal\DrupalExtension\Context\MessageContext
        - Drupal\DrupalExtension\Context\MarkupContext
        - Drupal\DrupalExtension\Context\RandomContext

  extensions:
    Drupal\MinkExtension:
      # Base URL of the Drupal site under test.
      base_url: http://mysite.local
      # Path to fixture files for file upload steps.
      files_path: '%paths.base%/tests/files'
      # Non-JavaScript browser driver.
      browserkit_http: ~
      # Default browser name.
      browser_name: chrome
      # JavaScript browser session.
      javascript_session: selenium2
      # Automatically open browser on failure.
      show_auto: false
      # Command to open the browser (e.g., "open %s" on macOS).
      show_cmd: ~
      # Temp directory for show_auto screenshots.
      show_tmp_dir: /tmp
      # Selenium2 driver configuration.
      selenium2:
        wd_host: 'http://localhost:4444/wd/hub'
        capabilities:
          browser: chrome
          extra_capabilities:
            'goog:chromeOptions':
              args:
                - '--disable-gpu'
                - '--disable-extensions'
                - '--no-first-run'
                - '--test-type'

    Drupal\DrupalExtension:
      # Default driver: "blackbox" for remote testing.
      default_driver: blackbox
      # API driver: "drupal" or "drush" (used when @api tag is present).
      api_driver: drush
      # Drush driver variant (legacy setting).
      drush_driver: drush
      # Suppresses the "[Deprecation] ..." notices written to STDERR. The
      # "BEHAT_DRUPALEXTENSION_SUPPRESS_DEPRECATIONS" environment variable
      # overrides this setting in either direction.
      suppress_deprecations: false
      # Field-value parser used by parseEntityFields(): "default" or "legacy".
      field_parser: default
      # User entity property submitted as the login value (e.g. "name", "mail").
      login_field: name
      # Enable the blackbox driver.
      blackbox: ~
      # Map of named regions to CSS selectors. Region steps such as
      # 'I press :button in the :region region' resolve against this map.
      regions:
        Content: '#main .region-content'
        Header: '#header'
        Footer: '#footer'
        Left sidebar: '#sidebar-first'
        Right sidebar: '#sidebar-second'
      # Seconds to wait for the browser to load after login. 0 to disable.
      login_wait: 0
      # Maximum time (in seconds) to wait for AJAX calls to complete.
      ajax_timeout: 5
      # Text strings used in step definitions.
      text:
        login_url: /user
        logout_url: /user/logout
        logout_confirm_url: /user/logout/confirm
        log_in: Log in
        log_out: Log out
        password_field: Password
        username_field: Username
      # CSS selectors for Drupal UI elements, grouped by concern. Message
      # selectors live under 'messages:' (keys: default, error, success,
      # warning); other selectors stay flat at the top of the map.
      selectors:
        messages:
          default: '.messages'
          error: '.messages--error'
          success: '.messages--status'
          warning: '.messages--warning'
        login_form_selector: 'form#user-login,form#user-login-form'
        logged_in_selector: 'body.logged-in,body.user-logged-in'

# Drupal API profile: tests that bootstrap Drupal.
drupal:
  suites:
    default:
      contexts:
        - Drupal\DrupalExtension\Context\DrupalContext
        - Drupal\DrupalExtension\Context\MinkContext
        - Drupal\DrupalExtension\Context\MessageContext
        - Drupal\DrupalExtension\Context\MarkupContext
        - Drupal\DrupalExtension\Context\DrushContext
        - Drupal\DrupalExtension\Context\BatchContext
        - Drupal\DrupalExtension\Context\MailContext
        - Drupal\DrupalExtension\Context\RandomContext
        - Drupal\DrupalExtension\Context\ConfigContext

  extensions:
    Drupal\MinkExtension:
      base_url: http://mysite.local
      files_path: '%paths.base%/tests/files'

    Drupal\DrupalExtension:
      api_driver: drupal
      login_field: name
      # Drupal API driver settings.
      drupal:
        drupal_root: /var/www/html/web
      # Drush driver settings.
      drush:
        alias: ~
        binary: vendor/bin/drush
        root: /var/www/html/web
        global_options: ~
      regions:
        Content: '#main .region-content'
        Header: '#header'
        Footer: '#footer'
        Left sidebar: '#sidebar-first'
        Right sidebar: '#sidebar-second'
      selectors:
        messages:
          default: '.messages'
          error: '.messages--error'
          success: '.messages--status'
          warning: '.messages--warning'
        login_form_selector: 'form#user-login,form#user-login-form'
        logged_in_selector: 'body.logged-in,body.user-logged-in'
