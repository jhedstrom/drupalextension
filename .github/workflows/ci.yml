name: ci
on: [push, pull_request]
jobs:
  tests:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        include:
          - php_version: "7.4"
            drupal_version: "9"
          - php_version: "8.2"
            drupal_version: "10"
          - php_version: "8.3"
            drupal_version: "10"
          - php_version: "8.3"
            drupal_version: "11"
    env:
      PHP_VERSION: ${{ matrix.php_version }}
      DRUPAL_VERSION: ${{ matrix.drupal_version }}
      DOCKER_USER_ID: "1001"
    steps:
      - name: clone
        uses: actions/checkout@v3

      - name: 🔐 Fix Git Dubious Ownership on Host
        # Use the host's git to globally trust the workspace directory.
        # This tells Git not to worry about the ownership mismatch in the workspace.
        run: git config --global --add safe.directory "$GITHUB_WORKSPACE"

      - name: docker compose up -d
        run: docker compose up -d
      - name: npm install
        run: docker compose exec -u ${DOCKER_USER_ID} -T -u node node npm install
      - name: composer self-update
        run: docker compose exec -u ${DOCKER_USER_ID} -T php composer self-update
      - name: composer require
        run: docker compose exec -u ${DOCKER_USER_ID} -T php composer require --no-interaction --dev --no-update drupal/core:^${DRUPAL_VERSION} drupal/core-composer-scaffold:^${DRUPAL_VERSION}
      - name: composer install
        run: docker compose exec -u ${DOCKER_USER_ID} -T php composer install
      - name: drush site-install
        run: docker compose exec -u ${DOCKER_USER_ID} -T php ./vendor/bin/drush --yes --root=drupal site-install --db-url=mysql://drupal:drupal@db/drupal --debug
      - name: copy fixtures
        run: docker compose exec -u ${DOCKER_USER_ID} -T php cp -r fixtures/drupal/modules/behat_test drupal/modules
      - name: drush pmu page_cache
        run: docker compose exec -u ${DOCKER_USER_ID} -T php ./vendor/bin/drush --yes --root=drupal pmu page_cache,big_pipe
      - name: drush en behat_test
        run: docker compose exec -u ${DOCKER_USER_ID} -T php ./vendor/bin/drush --yes --root=drupal en behat_test
      - name: npm test
        run: docker compose exec -u ${DOCKER_USER_ID} -T -u node node npm test
      - name: composer test
        run: docker compose exec -u ${DOCKER_USER_ID} -T php composer test
      - name: Debug Feature File Content
        run: |
          echo "Behat version"
          docker compose exec -u ${DOCKER_USER_ID} -T php vendor/bin/behat -V

          echo "Find behat.yml"
          docker compose exec -u ${DOCKER_USER_ID} -T php find . -name "behat.yml" -exec echo "behat.yml found at: {}" \; -exec cat {} \;

        shell: bash
      - name: Sanitize Feature File (Replace NBSP with Space)
        run: |
          docker compose exec -u ${DOCKER_USER_ID} -T php sh -c "ls -la  features/"
          docker compose exec -u ${DOCKER_USER_ID} -T php sh -c "whoami"
          # Replace all Non-Breaking Space characters (hex C2 A0) with standard Space (hex 20)
          # The LANG=C ensures sed treats bytes literally, preventing locale-related issues.
          docker compose exec -u ${DOCKER_USER_ID} -T php sh -c "ls -la  features/"
        shell: bash
      - name:  Debug Feature File Content (Focus on Line 2)
        run: |
          echo "----behayt.yml.dist----"
          cat behat.yml.dist
          echo "----end behayt.yml.dist----"

          echo "Behat version"
          docker compose exec -u ${DOCKER_USER_ID} -T php vendor/bin/behat -V

          ls -la

        shell: bash

      - name: behat --profile=blackbox
        run: docker compose exec -u ${DOCKER_USER_ID} -T php vendor/bin/behat --tags='@blackbox' --strict
      - name: behat --profile=drupal9
        if: "${{ matrix.drupal_version == '9'}}"
        run: docker compose exec -u ${DOCKER_USER_ID} -T php cat && docker compose exec -u ${DOCKER_USER_ID} -T php vendor/bin/behat -fprogress --profile=drupal9 --strict
      - name: behat --profile=drupal10
        if: "${{ matrix.drupal_version != '9'}}"
        run: docker compose exec -u ${DOCKER_USER_ID} -T php cat && docker compose exec -u ${DOCKER_USER_ID} -T php vendor/bin/behat -fprogress --profile=drupal10 --strict
      - name: behat --profile=drupal_https
        if: "${{ matrix.drupal_version != '9'}}"
        run: docker compose exec -u ${DOCKER_USER_ID} -T php cat && docker compose exec -u ${DOCKER_USER_ID} -T php vendor/bin/behat -fprogress --profile=drupal_https --strict
